import React, {ChangeEvent, useState, useRef} from 'react';import s from './Css.module.css'function App() {    const [increment, setIncrement] = useState(0)    const [valueMax, setValueMax] = useState('5')    const [valueMin, setValueMin] = useState('0')    const [isEdit, setIsEdit] = useState(true)    const [error, setError] = useState('enter press set')    const onChangeMaxHandler = (e: ChangeEvent<HTMLInputElement>) => {        setValueMax(e.currentTarget.value)        setIsEdit(false)    }    const onChangeMinHandler = (e: ChangeEvent<HTMLInputElement>) => {        setValueMin(e.currentTarget.value)        setIsEdit(false)    }    const onClickApplyHandler = () => {        setIncrement(+valueMin)        setIsEdit(true)    }    const onClickIncrHandler = () => {        setIncrement(increment + 1)    }    const onClickResHandler = () => {        setIncrement(+valueMin)    }    return (        <div className={s.wrapper}>            <div className={`${s.settings} ${isEdit === true ? s.opacity : ""} `}>                <input value={valueMax} onChange={onChangeMaxHandler} type="number"/>                <input value={valueMin} onChange={onChangeMinHandler} type="number"/>                <button disabled={isEdit} onClick={onClickApplyHandler}>apply</button>            </div>            <div className={s.null}>            </div>            <div className={`${s.window} ${isEdit === false ? s.opacity : ""}`}>                <div className={s.wrapperSpan}>                    {isEdit                        ? <span> {increment}</span>                        : <span> {error}</span>                    }                </div>                <button disabled={!isEdit} onClick={onClickIncrHandler}>inc</button>                <button disabled={!isEdit} onClick={onClickResHandler}>res</button>            </div>        </div>    );}export default App;